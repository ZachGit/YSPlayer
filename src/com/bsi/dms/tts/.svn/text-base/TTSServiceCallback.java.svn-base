package com.bsi.dms.tts;

public interface TTSServiceCallback {
	/**
	 * Called when tts string is converted to audio data
	 * @param args TTS arguments
	 * @param soundData Audio data converted from tts string
	 */
	public void onTTSSoundData(TTSArguments args, byte[] soundData);
}
